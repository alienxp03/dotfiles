# wktree project config
# Generated by wktree init.

# worktree_dir: ~/workspace/worktrees
# tmux_mode: window
# workspace_mode: single

workspaces:
  - name: backend
    repo: .
    # files:
    #   copy:
    #     - .env.local
    #   symlink:
    #     - .tool-versions
    # hooks:
    #   post_create:
    #     - pnpm install
    # randomize_ports:
    #   - file: .env.local
    #     vars:
    #       - PORT
    #       - APP_PORT
    # set_env:
    #   - file: .env.local
    #     vars:
    #       API_URL: "http://localhost:${window_name:.env.local:PORT}/api"
    # open:
    #   - "http://localhost:${window_name:.env.local:PORT}"
    panes:
      - command: nvim
        focus: true
      - commands:
          - echo "Tab 1"
        split: horizontal
      - commands:
          - cox
        split: vertical

  - name: frontend
    repo: ~/Workspace/wktree
    panes:
      - command: nvim
        focus: true
      - commands:
          - echo "Tab 1"
        split: horizontal
      - commands:
          - pix
        split: vertical
# defaults:
#   files:
#     copy:
#       - .env
#     symlink:
#       - AGENTS.override.md
