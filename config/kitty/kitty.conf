# ─────────────────────────────────────────────────────────────────────────────
# General
# ─────────────────────────────────────────────────────────────────────────────
font_size 13
shell_integration no-title
editor nvim
startup_session ~/.config/kitty/startup-session.conf
scrollback_lines 10000
wheel_scroll_multiplier 3.0
copy_on_select yes
notify_on_cmd_finish unfocused 10.0
allow_remote_control yes
listen_on unix:/tmp/kitty

# Window appearance
hide_window_decorations titlebar-only
window_padding_width 1 5 2 2
background_opacity 0.83
background_blur 0
active_border_color #a6e22e
inactive_border_color #6e7066

# Tabs and layouts
enabled_layouts splits:split_axis=horizontal,stack
tab_bar_edge top
tab_bar_style powerline
tab_bar_filter session:~
# tab_title_template "{tab.active_wd.rsplit('/', 1)[-1]}"

# ─────────────────────────────────────────────────────────────────────────────
# Keyboard
# ─────────────────────────────────────────────────────────────────────────────
kitty_mod cmd+shift
macos_option_as_alt left

# Config and pane layout
map cmd+r load_config_file
map cmd+shift+f toggle_layout stack
map cmd+shift+r start_resizing_window
map cmd+d combine : goto_layout splits : launch --cwd=current --location=vsplit
map cmd+shift+d combine : goto_layout splits : launch --cwd=current --location=hsplit
map cmd+enter new_window_with_cwd
map cmd+t new_tab_with_cwd
map cmd+shift+n set_tab_title

# Session and project pickers
map cmd+shift+o launch --type=overlay ~/.config/kitty/scripts/kitty-zoxide-session.sh
map cmd+shift+p launch --type=background kitten quick-access-terminal --config ~/.config/kitty/quick-access-terminal-center.conf --instance-group project-picker ~/.config/kitty/scripts/kitty-zoxide-session.sh

# Tabs
map cmd+1 goto_tab 1
map cmd+2 goto_tab 2
map cmd+3 goto_tab 3
map cmd+4 goto_tab 4
map cmd+5 goto_tab 5
map cmd+6 goto_tab 6
map cmd+7 goto_tab 7
map cmd+8 goto_tab 8
map cmd+9 goto_tab 9
map cmd+shift+backslash toggle_tab title:.*

# Window navigation
map ctrl+h neighboring_window left
map ctrl+j neighboring_window down
map ctrl+k neighboring_window up
map ctrl+l neighboring_window right

# Window resizing
map cmd+shift+h resize_window wider
map cmd+shift+j resize_window taller
map cmd+shift+k resize_window shorter
map cmd+shift+l resize_window narrower

# ─────────────────────────────────────────────────────────────────────────────
# Monokai Classic colors
# ─────────────────────────────────────────────────────────────────────────────
background #272822
foreground #fdfff1
cursor #c0c1b5
cursor_text_color #8d8e82
selection_background #57584f
selection_foreground #fdfff1

color0 #272822
color1 #f92672
color2 #a6e22e
color3 #e6db74
color4 #fd971f
color5 #ae81ff
color6 #66d9ef
color7 #fdfff1
color8 #6e7066
color9 #f92672
color10 #a6e22e
color11 #e6db74
color12 #fd971f
color13 #ae81ff
color14 #66d9ef
color15 #fdfff1

# Cursor trail
cursor_trail 1
cursor_trail_decay 0.01 0.5
cursor_trail_start_threshold 0

# Scrollback in Neovim
scrollback_pager nvim --cmd 'set eventignore=FileType' +'nnoremap q ZQ' +'call nvim_open_term(0, {})' +'set readonly nolist' +'$' -
map kitty_mod+up scroll_line_up
map kitty_mod+down scroll_line_down
map kitty_mod+page_up scroll_page_up
map kitty_mod+page_down scroll_page_down
map kitty_mod+home scroll_home
map kitty_mod+end scroll_end
map kitty_mod+z show_scrollback
